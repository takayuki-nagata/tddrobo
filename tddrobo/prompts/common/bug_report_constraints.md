- Focus strictly on the **currently failing test cases** in `<test_output>` (do not analyze previously passing tests or speculate on unrelated logic).
- Keep the `expected_vs_actual` analysis extremely concise (under 100 words or 2-3 sentences). Focus strictly on the exact discrepancies. Do NOT copy long tracebacks or stack traces inside the JSON.
- Keep the `fix_instructions` highly focused and action-oriented. Explain what logic is incorrect and how to fix it in 2-4 sentences. Do NOT include large code snippets or repeat long outputs inside the JSON.
- Do NOT write step-by-step trace thoughts, stream-of-consciousness reasoning, or internal debugging rambling inside the final JSON output fields (use the model's internal thinking phase for reasoning instead; the final output fields must contain only clean, structured, and actionable guidance).
- **Architectural Bug Resolution**: If a bug is caused by a syntax conflict, incorrect interface definitions, or missing validation boundaries in the Design Document (such as variables incorrectly matching constant patterns), do NOT propose ad-hoc runtime code hacks, dynamic bypasses, or coupled helper functions in the code. Instead, direct the fix towards resolving the structural mismatch (e.g., updating the validation regex or correcting the data schema) and recommend updating the design document (`target_to_fix = "generate_design"`).
- **Data Classification and Routing Audit**: Verify whether the inputs, tokens, patterns, or data formats are correctly classified and routed. Check if a letter or variable name is being misclassified under constant/numeric patterns (or vice versa) in the lexer/parser, leading to downstream processing failures.
- **Comprehensive Diagnostics Scrutiny**: Inspect both standard output and standard error (including internal tracebacks or exception messages swallowed by test assertions) to determine if syntax routing issues (such as parser token mismatches) are the root cause.

