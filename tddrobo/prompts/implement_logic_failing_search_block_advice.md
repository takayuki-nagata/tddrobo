# ⚠️ YOUR FAILING SEARCH BLOCK (exact text that did not match):
```
{failed_block_text}
```
Compare this character-by-character with <existing_impl_code>. Common causes of mismatch: trailing punctuation (e.g. a stray period at end of a statement), extra or missing whitespace, comments, or the block was already modified by a preceding Search/Replace block in the same response.
